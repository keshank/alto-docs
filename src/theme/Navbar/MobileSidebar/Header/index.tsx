import {type ReactNode} from 'react';

/**
 * The drawer rolls down from beneath the existing navbar — the navbar stays
 * visible as the "header" for the open drawer, and its hamburger morphs into
 * an X to close. So the drawer renders no header of its own.
 */
export default function NavbarMobileSidebarHeader(): ReactNode {
  return null;
}
